apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nodeapp-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nodeapp
    spec:
      containers:
      - name: nodeapp
        image: dckreg:5000/nodeapp:1.1
        ports:
        - containerPort: 3000
